# Exa MCP Server - Docker Compose
# For self-hosting on VPS

services:
  mcp-exa:
    build: .
    container_name: mcp-exa
    restart: unless-stopped
    ports:
      - "8087:8080"
    environment:
      - PORT=8080
      - HOST=0.0.0.0
      - EXA_API_KEY=${EXA_API_KEY:-}
      - ENABLED_TOOLS=${ENABLED_TOOLS:-}
      - DEBUG=${DEBUG:-false}
      - NODE_ENV=production
      - ANALYTICS_DIR=/app/data
      - ANALYTICS_IMPORT_KEY=${ANALYTICS_IMPORT_KEY:-}
      - KEY_SERVICE_URL=${KEY_SERVICE_URL:-}
      - KEY_SERVICE_TOKEN=${KEY_SERVICE_TOKEN:-}
    volumes:
      - analytics-data:/app/data
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - mcp-network
      - key-service
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  mcp-network:
    driver: bridge
  key-service:
    external: true
    name: mcp-network

volumes:
  analytics-data:
    driver: local
